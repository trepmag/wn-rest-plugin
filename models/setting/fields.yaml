# ===================================
#  Form Field Definitions
# ===================================

fields:
    prefix:
        label: trepmag.rest::lang.settings.prefix_label
        commentAbove: trepmag.rest::lang.settings.prefix_comment
        default: 'api/v1/'