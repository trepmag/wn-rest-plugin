# ===================================
#  List Column Definitions
# ===================================

columns:
    path:
        label: trepmag.rest::lang.node.path_label
        searchable: true

    is_enabled:
        label: trepmag.rest::lang.node.enabled
        type: partial
        path: is_enabled
        sortable: false
